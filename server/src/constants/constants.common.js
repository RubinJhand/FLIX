import ENV from '../utils/env';

export const NODE_ENV = ENV.NODE_ENV;
export const PORT = ENV.PORT || '8000';

export const API_CURRENT_VERSION = `/api/v1`;
export const DB_REFRESH_PATH = '/re/fr/esh';
